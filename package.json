{
  "name": "@ludlovian/retry",
  "version": "0.1.3",
  "description": "Retry async function",
  "main": "src/index.mjs",
  "exports": {
    ".": "./src/index.mjs"
  },
  "scripts": {
    "clean": "rm -rf coverage/ node_modules/.cache/",
    "lint": "standard '{src,test}/**/*.mjs'|snazzy",
    "format": "prettier-standard '{src,test}/**/*.mjs'",
    "test": "c8 node --test",
    "coverage": "npm run test && c8 report --reporter=html && sirv coverage/ -p 8000 -H"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ludlovian/retry.git"
  },
  "author": "",
  "license": "Unlicense",
  "bugs": {
    "url": "https://github.com/ludlovian/retry/issues"
  },
  "homepage": "https://github.com/ludlovian/retry#readme",
  "dependencies": {
    "@ludlovian/timeout": "link:../timeout"
  }
}
